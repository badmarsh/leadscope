events {}
http {
    server {
        listen 80;
        location / {
            proxy_pass http://host.docker.internal:8046;
            proxy_set_header Host 127.0.0.1:8046;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
